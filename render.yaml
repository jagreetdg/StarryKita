services:
  - type: web
    name: starry-kita-backend
    env: node
    rootDirectory: server
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
